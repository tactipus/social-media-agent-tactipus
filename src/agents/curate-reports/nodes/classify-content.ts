import { CurateReportsState } from "../state.js";

/**
 * Classify the content as relevant/interesting or not. Returns new data to process.
 * @param state
 */
export async function classifyContent(state: CurateReportsState) {
  throw new Error("Not implemented" + state);
}
